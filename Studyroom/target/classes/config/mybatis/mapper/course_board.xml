<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cb">
	<!-- 코스 전체 공지글 조회 -->
	<select id="board_list" resultType="courseboard" parameterType="java.util.HashMap">
	 	SELECT * FROM course_board
	 	WHERE COURSE_ID = #{course_id}
	 	ORDER BY register_date DESC
	</select>
	
	<!-- 코스 전체 공지글 수 조회 -->
	<select id="board_count" resultType="int">
		SELECT COUNT(*) FROM course_board
		WHERE COURSE_ID = #{course_id}
	</select>
	
	<!-- 코스 공지글 한 건 조회 -->
	<select id="board_one" parameterType="int" resultType="courseboard">
		SELECT * FROM course_board 
		WHERE COURSE_ID = #{course_id}
	</select>

	<!-- 코스 공지글 추가하기 -->
	<insert id="board_insert" parameterType="courseboard">
		INSERT INTO course_board VALUES(
			SEQ_COURSE_BOARD_ID.nextVal,
			#{course_id},
			#{title},
			#{content},
			sysdate,
			#{file_name},
			0
		)
	</insert>

	<!-- 코스 공지글 수정하기 -->
	<update id="board_modify" parameterType="courseboard">
		UPDATE course_board 
		SET TITLE = #{title},
			CONTENT = #{content},
			FILE_NAME = #{file_name}
		WHERE ID = #{id}				
	</update>

	<!-- 코스 공지글 삭제한 것처럼 수정 -->
	<update id="board_delete_update" parameterType="int">
		UPDATE course_board 
		SET DEL_FLAG = -1
		WHERE ID = #{id}
	</update>

	<!-- 코스 공지글 물리적 삭제 -->
	<delete id="board_delete" parameterType="int">
		DELETE FROM course_board 
		WHERE ID = #{id}
	</delete>
</mapper>

